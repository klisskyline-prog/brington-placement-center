# Cloudflare Worker / backend email endpoint.
# Example: https://placement-email.your-subdomain.workers.dev/submit-placement
VITE_PLACEMENT_API_URL=

# Show Download Backup JSON / Open Email Draft fallback buttons.
# Keep false/empty for production. Set true only for local testing.
VITE_SHOW_TEST_BACKUP_ACTIONS=
